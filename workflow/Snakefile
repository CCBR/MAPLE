from os.path import join
include: join("rules","init.smk")

localrules: all
rule all:
    input: 
        expand(join(fq_dir,'{sample_id}_R1.fastq.gz'),sample_id=SAMPLES),
        expand(join(fq_dir,'{sample_id}_R2.fastq.gz'),sample_id=SAMPLES),
        #expand(join(trim_dir,'{sample_id}_R1.trimmed.fastq.gz'),sample_id=sample_list),
        #expand(join(trim_dir,'{sample_id}_R1.trimmed.fastq.gz'),sample_id=sample_list),

include: join("rules","pre-process.smk")