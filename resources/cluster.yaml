
# cluster configuration
__default__:
    gres: lscratch:96
    mem: 40g
    partition: ccr,norm
    time: 00-08:00:00
    threads: 32
    output: ${{SLURM_JOBID}}.%j.{params.rname}.{wildcards}.out
    error: ${{SLURM_JOBID}}.%j.{params.rname}.{wildcards}.err